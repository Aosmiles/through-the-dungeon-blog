---
import GalleryImage from "./GalleryImage.astro";

const { illustrations } = Astro.props;
---

<ul class="columns-1 md:columns-2 lg:columns-3 xl:columns-4 gallery">
  {
    illustrations.map((i) => (
      <li class="mb-5">
        <GalleryImage illustration={i} />
      </li>
    ))
  }
</ul>
