---
interface Props {
  href: string;
}

const { href } = Astro.props;
const currentPage = Astro.url.pathname === href;
---

<a
  class:list={["hover:text-riso-red block", { underline: currentPage }]}
  href={href}><slot /></a
>
