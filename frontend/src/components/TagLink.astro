---
import risoColorClasses from "../utility/risoColorClasses";
const { color, slug, title } = Astro.props;
---

<a
  class:list={["block text-white text-sm px-3", risoColorClasses[color].bg]}
  href={`/blog/${slug}`}
>
  {title}
</a>
