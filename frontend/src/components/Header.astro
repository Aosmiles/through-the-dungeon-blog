---
import { getSiteSettings } from "../lib/sanity";
import NavBar from "./NavBar.astro";

const settings = await getSiteSettings();
const hidden = Astro.url.pathname !== "/";
---

<NavBar />
<header class="container mx-auto">
  <!-- <h1
    class:list={["text-riso-red font-bold uppercase text-center", { hidden }]}
  >
    {settings.title}
  </h1> -->
  <h1 class="md:text-center text-riso-red uppercase md:font-bold md:text-3xl">
    {settings.title}
  </h1>
  <div class="mt-6 border-b-2 border-riso-yellow"></div>
</header>
